#!/usr/bin/env bash
# the shebang is ignored, but nice for editors
export VIRTUAL_ENV=."venv"
layout python


# Database
export DATABASE_URL="postgresql+asyncpg://postgres:postgres@localhost:5432/wedding_rsvp"
export DATABASE_URL_SYNC="postgresql://postgres:postgres@localhost:5432/wedding_rsvp"

# JWT
export SECRET_KEY="your-super-secret-key-change-in-production"
export ALGORITHM="HS256"
export ACCESS_TOKEN_EXPIRE_MINUTES=60*24*7  # 7 days

# Email
export SMTP_HOST="localhost"
export SMTP_PORT=1025
export SMTP_USER=""
export SMTP_PASSWORD=""
export EMAILS_FROM="wedding@example.com"

# App
export APP_HOST="0.0.0.0"
export APP_PORT=8000
export DEBUG=true
export FRONTEND_URL="http://localhost:4321"

